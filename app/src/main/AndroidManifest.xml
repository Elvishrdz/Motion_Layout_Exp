<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.eahm.motionlayoutexperiments">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.Basics"></activity>
        <activity android:name=".ui.MotionWithCode" />
        <activity android:name=".ui.DragEventAndComplexPaths" />
        <activity android:name=".ui.ModifyingAPath" />
        <activity android:name=".ui.DragEventAnimation" />
        <activity android:name=".ui.TouchingAStar" />
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>